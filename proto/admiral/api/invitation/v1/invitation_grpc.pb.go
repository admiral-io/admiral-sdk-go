// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: admiral/api/invitation/v1/invitation.proto

package invitationv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	InvitationAPI_CreateInvitation_FullMethodName = "/admiral.api.invitation.v1.InvitationAPI/CreateInvitation"
	InvitationAPI_ListInvitations_FullMethodName  = "/admiral.api.invitation.v1.InvitationAPI/ListInvitations"
	InvitationAPI_RevokeInvitation_FullMethodName = "/admiral.api.invitation.v1.InvitationAPI/RevokeInvitation"
)

// InvitationAPIClient is the client API for InvitationAPI service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// InvitationAPI brings a person into the caller's tenant.
//
// An invitation is its own resource: it has a lifecycle (PENDING, ACCEPTED,
// EXPIRED, REVOKED) and a user exists only once it is redeemed. Creating one
// sends the mail; the link in it leads to the sign-in service, which is where
// the person proves the address and joins. The token in that link never
// appears on this surface.
//
// Keyed on the address, one live invitation per address. Creating a second one
// for the same address retires the first and mails a new link, which is also
// how "send it again" works; there is no resend procedure.
//
// Every request acts on the caller's own tenant; none names one.
type InvitationAPIClient interface {
	// CreateInvitation invites an address into the caller's tenant for a role and
	// sends the mail. Returns the invitation; the link is only ever in the mail.
	//
	// A live invitation for the same address is retired and replaced, so a retry
	// or a "send it again" is this same call. ALREADY_EXISTS when the address is
	// already a member. INVALID_ARGUMENT for a role that cannot be granted by
	// invitation: OWNER moves by transfer, and APP_CREATOR is a capability
	// rather than a role.
	//
	// Scope: `user:write`
	CreateInvitation(ctx context.Context, in *CreateInvitationRequest, opts ...grpc.CallOption) (*CreateInvitationResponse, error)
	// ListInvitations returns a page of the caller's tenant's invitations, every
	// status included, so an administrator can see what is outstanding, what
	// expired and what was withdrawn.
	//
	// Scope: `user:read`
	ListInvitations(ctx context.Context, in *ListInvitationsRequest, opts ...grpc.CallOption) (*ListInvitationsResponse, error)
	// RevokeInvitation withdraws a PENDING invitation; the link in the mail stops
	// working at once. Idempotent: revoking one already revoked succeeds and
	// changes nothing. FAILED_PRECONDITION when it was already accepted: there is
	// a member now, and that is a different act.
	//
	// Scope: `user:write`
	RevokeInvitation(ctx context.Context, in *RevokeInvitationRequest, opts ...grpc.CallOption) (*RevokeInvitationResponse, error)
}

type invitationAPIClient struct {
	cc grpc.ClientConnInterface
}

func NewInvitationAPIClient(cc grpc.ClientConnInterface) InvitationAPIClient {
	return &invitationAPIClient{cc}
}

func (c *invitationAPIClient) CreateInvitation(ctx context.Context, in *CreateInvitationRequest, opts ...grpc.CallOption) (*CreateInvitationResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(CreateInvitationResponse)
	err := c.cc.Invoke(ctx, InvitationAPI_CreateInvitation_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *invitationAPIClient) ListInvitations(ctx context.Context, in *ListInvitationsRequest, opts ...grpc.CallOption) (*ListInvitationsResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ListInvitationsResponse)
	err := c.cc.Invoke(ctx, InvitationAPI_ListInvitations_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *invitationAPIClient) RevokeInvitation(ctx context.Context, in *RevokeInvitationRequest, opts ...grpc.CallOption) (*RevokeInvitationResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(RevokeInvitationResponse)
	err := c.cc.Invoke(ctx, InvitationAPI_RevokeInvitation_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// InvitationAPIServer is the server API for InvitationAPI service.
// All implementations should embed UnimplementedInvitationAPIServer
// for forward compatibility.
//
// InvitationAPI brings a person into the caller's tenant.
//
// An invitation is its own resource: it has a lifecycle (PENDING, ACCEPTED,
// EXPIRED, REVOKED) and a user exists only once it is redeemed. Creating one
// sends the mail; the link in it leads to the sign-in service, which is where
// the person proves the address and joins. The token in that link never
// appears on this surface.
//
// Keyed on the address, one live invitation per address. Creating a second one
// for the same address retires the first and mails a new link, which is also
// how "send it again" works; there is no resend procedure.
//
// Every request acts on the caller's own tenant; none names one.
type InvitationAPIServer interface {
	// CreateInvitation invites an address into the caller's tenant for a role and
	// sends the mail. Returns the invitation; the link is only ever in the mail.
	//
	// A live invitation for the same address is retired and replaced, so a retry
	// or a "send it again" is this same call. ALREADY_EXISTS when the address is
	// already a member. INVALID_ARGUMENT for a role that cannot be granted by
	// invitation: OWNER moves by transfer, and APP_CREATOR is a capability
	// rather than a role.
	//
	// Scope: `user:write`
	CreateInvitation(context.Context, *CreateInvitationRequest) (*CreateInvitationResponse, error)
	// ListInvitations returns a page of the caller's tenant's invitations, every
	// status included, so an administrator can see what is outstanding, what
	// expired and what was withdrawn.
	//
	// Scope: `user:read`
	ListInvitations(context.Context, *ListInvitationsRequest) (*ListInvitationsResponse, error)
	// RevokeInvitation withdraws a PENDING invitation; the link in the mail stops
	// working at once. Idempotent: revoking one already revoked succeeds and
	// changes nothing. FAILED_PRECONDITION when it was already accepted: there is
	// a member now, and that is a different act.
	//
	// Scope: `user:write`
	RevokeInvitation(context.Context, *RevokeInvitationRequest) (*RevokeInvitationResponse, error)
}

// UnimplementedInvitationAPIServer should be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedInvitationAPIServer struct{}

func (UnimplementedInvitationAPIServer) CreateInvitation(context.Context, *CreateInvitationRequest) (*CreateInvitationResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method CreateInvitation not implemented")
}
func (UnimplementedInvitationAPIServer) ListInvitations(context.Context, *ListInvitationsRequest) (*ListInvitationsResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method ListInvitations not implemented")
}
func (UnimplementedInvitationAPIServer) RevokeInvitation(context.Context, *RevokeInvitationRequest) (*RevokeInvitationResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method RevokeInvitation not implemented")
}
func (UnimplementedInvitationAPIServer) testEmbeddedByValue() {}

// UnsafeInvitationAPIServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to InvitationAPIServer will
// result in compilation errors.
type UnsafeInvitationAPIServer interface {
	mustEmbedUnimplementedInvitationAPIServer()
}

func RegisterInvitationAPIServer(s grpc.ServiceRegistrar, srv InvitationAPIServer) {
	// If the following call panics, it indicates UnimplementedInvitationAPIServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&InvitationAPI_ServiceDesc, srv)
}

func _InvitationAPI_CreateInvitation_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(CreateInvitationRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(InvitationAPIServer).CreateInvitation(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: InvitationAPI_CreateInvitation_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(InvitationAPIServer).CreateInvitation(ctx, req.(*CreateInvitationRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _InvitationAPI_ListInvitations_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ListInvitationsRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(InvitationAPIServer).ListInvitations(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: InvitationAPI_ListInvitations_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(InvitationAPIServer).ListInvitations(ctx, req.(*ListInvitationsRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _InvitationAPI_RevokeInvitation_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(RevokeInvitationRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(InvitationAPIServer).RevokeInvitation(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: InvitationAPI_RevokeInvitation_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(InvitationAPIServer).RevokeInvitation(ctx, req.(*RevokeInvitationRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// InvitationAPI_ServiceDesc is the grpc.ServiceDesc for InvitationAPI service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var InvitationAPI_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "admiral.api.invitation.v1.InvitationAPI",
	HandlerType: (*InvitationAPIServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "CreateInvitation",
			Handler:    _InvitationAPI_CreateInvitation_Handler,
		},
		{
			MethodName: "ListInvitations",
			Handler:    _InvitationAPI_ListInvitations_Handler,
		},
		{
			MethodName: "RevokeInvitation",
			Handler:    _InvitationAPI_RevokeInvitation_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "admiral/api/invitation/v1/invitation.proto",
}
